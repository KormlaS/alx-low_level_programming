#include <stdio.h>

/**
 * main - Prints all possible cominations of three different digits,
 * in ascending order, separated a comma followed by a space.
 *
 * Return: Always 0.
 */
int main(void)
{
int digit1, digit2, digit3;

for (digit1 = 0; digit1 < 8; digit1 ++)
{
for (digit2 = digit1 + 1; digit2 < 9; digit2 ++)
{
  for (digit3 = digit2 + 1; digit3 < 10; digit 3 ++)
    